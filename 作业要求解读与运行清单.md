# 作业要求解读与运行清单（EuroSAT MLP）

本文档按 `hw1.pdf` 要求整理，目标是让你一份文档完成「实现 + 实验 + 报告 + 提交」全流程。

---

## 1. 作业要求总览（来自 hw1.pdf）

这次作业的核心任务是：  
使用 **NumPy + 自定义自动微分** 从零实现三层 MLP（输入层-隐藏层-输出层），在 `EuroSAT_RGB`（10 类地表覆盖）上完成分类训练与评估。

你需要满足三个高层约束：

- 不能使用 PyTorch / TensorFlow / JAX 这类自带自动微分的框架。
- 需要有完整实验链路：训练、验证、测试、超参数搜索、可视化分析。
- 报告和代码提交都要“可复现、可核查”，不是只交一份准确率数字。

---

## 2. 硬性技术要求逐条解读

### 2.1 自动微分与反向传播

- 要求你自己实现反向传播计算梯度，不能调用现成深度学习框架 autograd。
- 本项目对应模块在 `src/autograd.py`，损失与梯度链路在训练中被调用。

### 2.2 五个模块必须具备

作业要求里明确提到至少包含：

- 数据加载与预处理
- 模型定义
- 训练循环
- 测试评估
- 超参数查找

本项目均已覆盖：

- 数据：`src/data.py`
- 模型：`src/nn.py`
- 训练：`train.py` + `src/trainer.py`
- 测试：`evaluate.py`
- 超参：`search.py` + `src/searcher.py`

### 2.3 模型与优化细节要求

作业要求包含下列技术点，你报告里建议逐条对齐写：

- 隐藏层大小可调（`--hidden-dims`）
- 至少两种激活函数可切换（`--activation relu/sigmoid/tanh`）
- SGD 优化器
- 学习率衰减（Learning Rate Decay）
- 交叉熵损失（Cross-Entropy）
- L2 正则（Weight Decay）
- 依据验证集准确率自动保存最佳权重（best checkpoint）

### 2.4 测试要求

- 需要导入训练好的最佳权重进行独立测试集评估。
- 需要输出测试准确率（Accuracy）。
- 需要给混淆矩阵（Confusion Matrix）。

---

## 3. 你项目中对应的脚本与职责

- `train.py`：训练模型，输出训练/验证曲线，保存 `best/` 与 `last/` 权重。
- `evaluate.py`：加载最佳模型，在测试集上输出 `test_accuracy`、混淆矩阵、错例可视化。
- `visualize.py`：可视化第一层隐藏层权重（对应报告“权重恢复图像”要求）。
- `search.py`：执行网格/随机搜索，比较不同超参数组合性能。
- `tools/make_dummy_dataset.py`：快速冒烟，检查代码链路是否可运行（可选）。

---

## 4. 建议执行顺序（必须跑哪些文件）

推荐目录：

```bash
cd /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw
```

### 第一步（必跑）：训练

```bash
python train.py \
  --data-root /home/zhangzhiwei/deeplearning/homework1/EuroSAT_RGB \
  --output-dir /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/output/train_run_final \
  --hidden-dims 512 \
  --activation relu \
  --dropout 0.2 \
  --learning-rate 0.01 \
  --momentum 0.9 \
  --lr-decay 0.98 \
  --weight-decay 0.0005 \
  --max-grad-norm 5.0 \
  --early-stopping-patience 6 \
  --batch-size 128 \
  --epochs 30 \
  --seed 42
```

### 第二步（必跑）：测试评估

```bash
python evaluate.py \
  --data-root /home/zhangzhiwei/deeplearning/homework1/EuroSAT_RGB \
  --checkpoint-dir /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/output/train_run_final/best \
  --split-file /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/output/train_run_final/split.json \
  --output-dir /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/output/eval_run_final
```

### 第三步（必跑）：第一层权重可视化

```bash
python visualize.py \
  --checkpoint-dir /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/output/train_run_final/best \
  --output-dir /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/output/vis_run_final \
  --max-filters 64 \
  --cols 8
```

### 第四步（强烈建议）：超参数搜索（用于报告对比实验）

网格搜索：

```bash
python search.py \
  --data-root /home/zhangzhiwei/deeplearning/homework1/EuroSAT_RGB \
  --output-dir /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/output/search_grid_final \
  --search-config /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/configs/grid_search.json \
  --seed 42
```

随机搜索（可替代网格，二选一也可以）：

```bash
python search.py \
  --data-root /home/zhangzhiwei/deeplearning/homework1/EuroSAT_RGB \
  --output-dir /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/output/search_random_final \
  --search-config /home/zhangzhiwei/deeplearning/homework1/eurosat_mlp_hw/configs/random_search.json \
  --seed 42
```

---

## 5. 每一步产物怎么用于报告

训练阶段（`output/train_run_final/`）：

- `loss_curve.png`：展示训练/验证 Loss 变化（报告必备图）。
- `accuracy_curve.png`：展示验证准确率变化（报告必备图）。
- `history.csv` 或 `history.json`：可提取表格写“最佳 epoch、最佳 val acc”。
- `best/weights.npz`：报告里说明“最终测试使用 best checkpoint”。

评估阶段（`output/eval_run_final/`）：

- `test_metrics.json`：写测试集准确率的官方数字来源。
- `confusion_matrix.png`：对应作业“混淆矩阵”硬性要求。
- `misclassified_examples.png`：对应错例分析（Error Analysis）要求。

可视化阶段（`output/vis_run_final/`）：

- `first_layer_weights.png`：对应“第一层权重恢复图像并分析空间模式”要求。

超参搜索阶段（`output/search_*_final/`）：

- `search_results.csv`：可放进报告做超参数对比表。
- `best_trial.json`：说明“最终方案的超参数选择依据”。

---

## 6. 提交材料清单（对照 hw1.pdf）

你最终应提交并在报告中明确给出：

- 实验报告 PDF（包含模型、数据、实验结果和分析）。
- 报告中必须有：
  - 训练/验证 Loss 曲线
  - 验证集 Accuracy 曲线
  - 第一层权重可视化与讨论
  - 错例分析
  - 测试集结果与混淆矩阵
- 代码仓库 Public GitHub 链接（README 写清环境与运行方法）。
- 训练好模型权重下载地址（Google Drive 等），报告里明确写出链接。
- 确保 GitHub 与权重链接更新时间早于课程截止时间（deadline 以课程公告为准）。

---

## 7. 常见失分点与自检

- 只报告训练集指标，没有独立测试集结果。
- 没有给混淆矩阵，或只贴文字不贴图。
- 没有错例图，或错例没有分析原因。
- 没有第一层权重可视化，或只有图没有讨论。
- 只给最终准确率，没有训练/验证过程曲线。
- README 没写如何安装依赖、如何训练与评估。
- 报告里没有 GitHub 链接或权重下载链接。

---

## 8. 最终一页 Checklist

提交前请逐条勾选：

- [ ] 我使用的是 NumPy + 自定义自动微分，没有用深度学习框架 autograd。
- [ ] 我的代码包含数据、模型、训练、测试、超参搜索五类模块。
- [ ] 我已运行 `train.py` 并得到 `loss_curve.png` 与 `accuracy_curve.png`。
- [ ] 我已保存并使用 `best` 权重进行测试评估。
- [ ] 我已运行 `evaluate.py` 并得到测试准确率与 `confusion_matrix.png`。
- [ ] 我已生成并分析 `misclassified_examples.png`。
- [ ] 我已运行 `visualize.py` 并得到 `first_layer_weights.png`，且完成讨论。
- [ ] 我已完成至少一组超参数对比实验（建议 `search.py`）。
- [ ] 报告中已放入 GitHub Repo 链接和模型权重下载链接。
- [ ] README 中已写清环境依赖与复现命令。

---

## 附：快速命令可用性检查（提交前 30 秒自检）

```bash
python train.py --help
python evaluate.py --help
python visualize.py --help
python search.py --help
```
